# 2026-04-14T16:20:16Z — Woz: System Tray Support + Phase/MESS Cleanup

## Outcome: SUCCESS

### Deliverables
- **TrayIconService** with H.NotifyIcon.WinUI — animated radar-sweep icons (16×16, 4-frame rotation at 300ms interval)
- **Context menu** — Restore, Launch Session, Exit
- **Settings binding** — `MinimizeToTray` (defaults to `true`)
- **Window lifecycle** — `AppWindow.Hide()` on close (if tray enabled); real exit via `App.ExitApplication()`
- **Phase/MESS cleanup** — All references removed from codebase
- **Test file renames** — 3 test files renamed for clarity
- **New unit tests** — 12 tests for TrayIconService (icon generation, animation, context menu events)

### Build Status
- ✅ Build passed
- ✅ 676 tests passing
- ✅ Zero regressions

### Files Modified
- `src/SquadUplink/SquadUplink.csproj` — Added H.NotifyIcon.WinUI (v2.4.1), System.Drawing.Common
- `src/SquadUplink/Contracts/ITrayIconService.cs` — New interface
- `src/SquadUplink/Services/TrayIconService.cs` — Full implementation
- `src/SquadUplink/Models/AppSettings.cs` — `MinimizeToTray` property
- `src/SquadUplink/App.xaml.cs` — Tray initialization, `ExitApplication()` method
- `src/SquadUplink/MainWindow.xaml.cs` — Close handler
- `src/SquadUplink/ViewModels/SettingsViewModel.cs` — Binding
- `src/SquadUplink/Views/SettingsPage.xaml` — UI section
- `src/SquadUplink/Helpers/ServiceCollectionExtensions.cs` — DI registration
- `tests/SquadUplink.Tests/Services/TrayIconServiceTests.cs` — 12 unit tests
- All Phase/MESS references removed

### Technical Notes
- H.NotifyIcon.WinUI chosen for maturity and WinUI 3 compatibility (avoids fragile Win32 interop)
- Icons programmatically generated (System.Drawing) to avoid resolution issues
- Animation thread-safe via `DispatcherQueue.TryEnqueue`
- If tray initialization fails, app continues without tray (graceful degradation)

### Next Steps
- Ready for merge to `main`
- Tray feature is additive; no breaking changes
