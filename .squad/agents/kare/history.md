# Project Context

- **Owner:** Brady
- **Project:** squad-uplink — A TypeScript/React/Vite front-end web app with a retro Apple IIe and Commodore 64 theme to control Squad agents remotely using the squad-rc feature and devtunnel. Will be hosted in Azure Static Web Apps.
- **Stack:** TypeScript, React, Vite, Azure Static Web Apps, devtunnel
- **Created:** 2026-04-05

## Core Context

Frontend Dev for squad-uplink. Responsible for React UI components, retro Apple IIe and Commodore 64 visual design, CSS architecture, theme system, and component library.

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

- **5-skin theme engine** (2026-04-05): Expanded from 2 to 5 skins. TerminalTheme now has optional fields for `layout`, `crtEnabled`, `customCss`, `chromeFontFamily`, `accentColors`, `borderSize`, `borderColor`. Theme cycling order is defined in `THEME_ORDER` constant in `src/themes/index.ts`.
- **Layout modes**: Three layout modes exist — `fullscreen` (CRT skins), `windowed` (Win95 with 98.css-style chrome), `panel` (LCARS grid with pill-shaped sidebars). Layout switching happens in `App.tsx`.
- **Win95 bg split**: Win95 has different desktop bg (#008080 teal) vs terminal bg (#000080 command prompt blue). This is intentional — the cross-theme consistency test accounts for this.
- **CRT conditional rendering**: CRTOverlay returns null for themes with `crtEnabled: false`. CSS flicker animation is also scoped per-theme.
- **Audio per skin**: `useAudio(skinId)` accepts a ThemeId parameter (backward-compatible default). Each skin has its own waveform, frequency, detune, and dual-tone profiles.
- **Font files not yet downloaded**: @font-face declarations are in place for IBM 3270, W95FA, Trek, and Mx437_IBM_3270. All use web-safe fallback chains. Actual .woff2 files need to be sourced from int10h.org and other sources.
- **2026-04-05 (Cross-Agent):** Jobs locked 6 architecture decisions + 6 milestones. Key patterns: Zustand for state (works outside React), flat component tree, CSS-only CRT overlays, ConnectionManager outside React, theme engine with paired CSS props + xterm ITheme swap. MVP criteria: 5 things (xterm + CRT + WebSocket + theme toggle + HITL). Kare's 5-skin expansion (Apple, C64, IBM 3270, Win95, LCARS) will extend theme engine — foundation: CSS custom properties + ThemeContext + xterm ITheme swap. Resource links provided (typography, CSS frameworks, audio assets).
- **Wave 2 completion** (2026-04-05): 5-skin theme engine fully implemented and tested. 150 theme tests passing. All 5 themes integrate cleanly with existing hooks/components. Hertzfeld identified critical reconnect backoff bug (retry counter resets on every reconnect call, preventing exponential escalation). Bug assigned to Woz for M1 work. Font files still need external sourcing.
- **HITL features** (2026-04-05): Expanded audio from 5 to 12 sound types with `SoundSequence` for multi-step sounds (boot chords, arpeggios). MechanicalSwitch has 3 variants (lever/checkbox/pill) keyed to theme. CRT toggle state stored in localStorage, propagated via prop to CRTOverlay and `.crt-screen` class. AudioToggle mute persists in localStorage via useAudio hook. All 151 tests pass after changes.
- **Wave 6 font optimization** (2026-04-05): Moved @font-face URLs from `src/assets/fonts/` to `public/fonts/` (Vite static serving). Added robust fallback chains per theme: apple2e→Apple II, c64→PetMe, ibm3270→IBM Plex Mono, win95→Fixedsys→Courier New, lcars→Antonio. Font preload hint in `index.html` for default theme. Created `public/fonts/README.md` with download/licensing instructions. All 6 declarations use `font-display: swap`.
- **Wave 6 accessibility** (2026-04-05): Added `src/styles/accessibility.css` with focus-visible indicators per theme (phosphor glow for CRT themes, dotted outline for Win95, orange ring for LCARS), `prefers-reduced-motion` media query disabling all animations/transitions, `.sr-only` utility class, and `.skip-link`. Added `aria-label`, `aria-pressed`, `aria-live`, `role="status"`, `role="application"` across all interactive components. Terminal has `aria-roledescription="terminal"`. StatusBar has `aria-live="polite"` on connection indicator. Theme changes announced via hidden `aria-live` region. Escape key returns focus to terminal. Controls toolbar has `role="toolbar"`. 293 tests passing.
- **2026-04-05 (Wave 4 cross-agent update from Woz):** TelemetryDrawer shipped — modern dark glass UI panel ("the mask slips" design moment). Connection metrics (latency, msg/s, uptime, reconnect count), session info, agent roster, auto-refresh /status every 30s. Keyboard: Ctrl+Shift+T open, Escape/backdrop close. Zustand `connectionStore` extended with `TelemetryMetrics`, `fetchStatus()` on ConnectionManager. CSS overlay (z-index 9999) works on all 5 theme layouts. 283 tests passing.
- **2026-04-05 (Wave 4+6 cross-agent update from Hertzfeld):** 81 tests written — 21 TelemetryDrawer (skipped pending Woz impl), 32 fonts (passing), 28 accessibility (passing). Font tests validate @font-face declarations, fallback chains, graceful degradation. A11y tests cover keyboard nav (Tab/Escape), ARIA attributes, reduced motion. Zustand updates wrapped in `act()` for React 19 compatibility. Font stack divergence documented.
- **Hybrid audio system** (2026-04-05): Reworked `useAudio` from procedural-only to hybrid model. New files: `src/audio/manifest.ts` (skin→sound→file path mapping), `src/audio/bufferCache.ts` (`AudioBufferCache` class with per-skin preloading). Hook tries cached `AudioBuffer` sample first, falls back to procedural oscillators. Preloads current skin's files on skin change via `useEffect`. Shared `AudioBufferCache` singleton survives re-renders. Directory structure `public/audio/{skinId}/` created for all 5 skins. Mock updated with `MockAudioBufferSourceNode` and `createMockAudioFetch`. Hook API unchanged (`{ play, muted, toggleMute }`). 323 tests pass.
- **2026-04-05 (Wave audio rework — implementation complete):** Delivered full hybrid audio system. Brady's directive to use real audio files (URLs provided per skin) overrode Jobs' "No sample files" decision. New modules: `src/audio/manifest.ts` (skin ID + sound type → file path), `src/audio/bufferCache.ts` (singleton `AudioBufferCache` with `fetch`/`decodeAudioData`/caching). `useAudio` hook refactored internally — public API unchanged, tries cached `AudioBuffer` first, falls back to procedural oscillators if file missing/failed/not yet loaded. Preloads only current skin's files on skin change (not all 5). Non-blocking playback: procedural plays immediately while file loads. Zero breaking changes. Graceful degradation: app fully functional if all audio files missing. `public/audio/{skinId}/` directories created for all 5 skins (apple2e, c64, ibm3270, win95, lcars). Build clean, 323 tests passing. Hertzfeld wrote 15 spec-driven tests covering file loading, caching, manifest, hybrid playback, all in `describe.skip` pending audio files. Backward-compat tests (4) passing. Un-skip trigger: source real audio files and populate `public/audio/{skinId}/` dirs.
- **Win95 full chrome rework** (2026-04-05): Four-part rework of Win95 skin. (1) Text contrast: CSS overrides with `!important` scoped to `[data-theme='win95']` force `#000000` on ThemeToggle, AudioToggle, MechanicalSwitch, and StatusBar — needed because components use inline `style={{ color: theme.fg }}` where `theme.fg` is `#c0c0c0` (same as chrome bg). (2) Functional window buttons: React state `windowState: 'normal'|'minimized'|'maximized'|'closed'` in Win95Layout — minimize hides via `display:none` (keeps terminal mounted), maximize toggles `win95-window--maximized` class filling viewport minus 28px taskbar, close shows desktop. (3) Desktop icon: 📟 icon at top-left of teal desktop when closed, single-click selects (blue highlight), double-click reopens, keyboard Enter support. (4) Taskbar: `📟 SQUAD UPLINK` button appears when running, sunken border when visible, toggles minimize/restore; HH:MM clock updates every 60s on the right. Win95 statusbar now reads live Zustand `status` instead of hardcoded "Ready"/"Connected". StatusBar component removed from win95 children (duplicate controls). All 16 previously-skipped Win95Layout tests un-skipped and passing. 361/361 tests pass.(1) Text contrast: CSS overrides with `!important` scoped to `[data-theme='win95']` force `#000000` on ThemeToggle, AudioToggle, MechanicalSwitch, and StatusBar — needed because components use inline `style={{ color: theme.fg }}` where `theme.fg` is `#c0c0c0` (same as chrome bg). (2) Functional window buttons: React state `windowState: 'normal'|'minimized'|'maximized'|'closed'` in Win95Layout — minimize hides via `display:none` (keeps terminal mounted), maximize toggles `win95-window--maximized` class filling viewport minus 28px taskbar, close shows desktop. (3) Desktop icon: 📟 icon at top-left of teal desktop when closed, single-click selects (blue highlight), double-click reopens, keyboard Enter support. (4) Taskbar: `📟 SQUAD UPLINK` button appears when running, sunken border when visible, toggles minimize/restore; HH:MM clock updates every 60s on the right. Win95 statusbar now reads live Zustand `status` instead of hardcoded "Ready"/"Connected". StatusBar component removed from win95 children (duplicate controls). All 16 previously-skipped Win95Layout tests un-skipped and passing. 361/361 tests pass.
- **2026-04-05 (Win95 chrome production rework — Kare + Hertzfeld):** Win95 desktop environment fully implemented and tested. Kare delivered: (1) Text contrast fix — CSS `!important` overrides force black text on gray chrome for all controls; (2) Window state machine — React state in Win95Layout (`windowState: normal|minimized|maximized|closed`), minimize hides terminal via display:none preserving mount state and scroll position, maximize toggles full-screen minus 28px taskbar, close triggers desktop; (3) Desktop icon — 📟 emoji at top-left when closed, single-click selects (blue highlight), double-click or Enter reopens terminal; (4) Taskbar — 28px footer with `📟 SQUAD UPLINK` button (toggles minimize/restore), live HH:MM clock updating every 60s on right, reads current Zustand `connectionStore.status` for status text (replaces hardcoded StatusBar). StatusBar component removed from win95 children (was duplicate). Hertzfeld delivered comprehensive test suite: 23 new tests covering window chrome buttons (8), desktop icon (4), taskbar (6), terminal preservation (3), and status integration (2). All tests un-skipped and passing. Win95 now feature-complete: functional chrome, readable text, desktop environment with icon and taskbar, terminal state preservation through minimize/restore cycles. Final metrics: 361/361 tests passing, 0 skipped, build clean. Win95 production-ready alongside other 4 skins.
- **Pip-Boy 3000 theme** (2026-04-05): 6th theme added — Fallout-inspired Pip-Boy 3000 with new `'pipboy'` layout mode. PipBoyLayout in App.tsx with 5-tab navigation (STAT/INV/DATA/MAP/RADIO), DATA tab default with xterm.js terminal always mounted (CSS display:none for tab switching, not conditional rendering). Green-on-black CRT (#1bff80 on #000500), custom `pipboy-flicker` animation (bypasses shared crt-flicker), scanline overlay, phosphor blur via backdrop-filter, boot warm-up glow animation. VT323 + Share Tech Mono font stack (2 new @font-face in fonts.css). Audio profile: sine chirps + square wave clicks + rising sweep boot. Full ThemeId union expansion from 5→6 required updates to ThemeToggle labels, MechanicalSwitch variants, useTheme THEME_MAP, THEME_ORDER, audio manifest, boot messages, global.css body styles, crt-effects.css. Tests updated for 6-theme cycle. Build clean, 374/374 passing (25 PipBoyLayout tests pre-skipped by Hertzfeld pending implementation — ready to un-skip). Placeholder tabs for Woz to fill with data components.
- **Pip-Boy data integration** (2026-04-05): Extended Zustand store with S.P.E.C.I.A.L. metrics (tokenUsage→intelligence, messageCount→luck, successCount→perception, latency→strength, throughput→perception). Added messageHistory (50-entry ring buffer), tools, mcpServers, activeAgent, commandHistory (10-entry ring buffer), uplinkOverride to ConnectionStore. Wired ConnectionManager to track both inbound and outbound messages with timestamps, agent attribution, content preview, raw payload. All 5 Pip-Boy components built and integrated into tabs. Graceful disconnected states render Pip-Boy-themed empty messages. CSS uses `pipboy-` prefix convention. Data tab displays message history with raw JSON toggle + auto-scroll. Radio tab supports command console with history navigation + quick buttons + uplink override input. 374 tests passing, build clean.
- **Pip-Boy full device chrome** (2026-04-05): Rebuilt PipBoyLayout from bare green terminal to full hardware device filling viewport. CSS grid layout: left grip (ventilation grille via repeating-linear-gradient), center screen bezel (recessed CRT with inset shadows), right panel (RADS dial, TUNE knob with conic-gradient ridges, toggle switches, indicator strip). Device body uses tan/brown metal (#8B7355) with gradient highlights/shadows. Decorative elements: 6 screws with cross-slot pseudo-elements, "PIP-BOY" stamped label, top clip/latch, bottom band with ventilation slots and glowing amber POWER button (#ffb641 with pulse animation). Created VaultBoy.tsx SVG component with standing figure (thumbs-up pose), limb damage health bars (5-segment per limb), and idle bounce animation. VaultBoy integrated into PipBoyStat with limb health derived from telemetry metrics. Responsive: side panels hide on ≤768px. All existing functionality preserved — tabs, terminal, data components, status bar all work inside the screen bezel. 399/399 tests passing, build clean.
- **2026-04-05 (Wave 7 — Pip-Boy full device chrome delivery):** Wave 7 completion. Kare delivered full Pip-Boy 3000 hardware device aesthetics: tan/brown metal casing (#8B7355), CSS grid 3-column layout (left grip | center bezel | right panel), recessed CRT screen well with inset box-shadow, left grip ventilation grille (repeating-linear-gradient), right panel RADS dial (rotatable CSS circle), TUNE knob (conic-gradient ridges), decorative screws (6 CSS pseudo-elements), embossed "PIP-BOY" label, glowing amber POWER button (#ffb641 with pulse animation). Created VaultBoy.tsx SVG component with thumbs-up pose, 5-segment limb health bars (Head/L-Arm/R-Arm/L-Leg/R-Leg), idle bounce animation (2s cycle), health colors map to ConnectionStore telemetry (latency→strength, throughput→endurance, uptime→perception). Terminal stays mounted inside screen bezel; tab switches use CSS display:none (preserves xterm state). Responsive: side panels hide on ≤768px. All 5 data components work inside bezel. Hertzfeld un-skipped 25 PipBoyLayout tests (all passing). Woz fixed HMR warning by splitting useTheme hook + ThemeProvider into separate files. 399/399 tests passing, build clean, HMR warnings eliminated.
- **Pip-Boy Codepen exact port** (2026-04-05): COMPLETE REPLACEMENT of Pip-Boy CSS and layout to match https://codepen.io/stix/pen/KdJEwB exactly. Replaced CSS grid layout with the Codepen's fixed 630x400 `.pip` container using `transform: scale()` for responsive viewport fitting (calculated via `usePipBoyScale` hook, ratio 660x520). Ported ALL Codepen CSS faithfully: pipfront with rounded corners and gradient, top panel with "Pip-Boy" label via `::after`, 5 screws (screw1-5) with rotation pseudo-elements, screen-border (370x290 rounded) with screen (300x235 absolute), screen-reflection gradient overlay (opacity 0.07), scan line animation (`@keyframes pipboy-scan`), nav tabs with CSS `::before` content labels, left/right speakers with inset shadows, left-wheel (rotated 6deg) and right-wheel with tab-names list, RADS meter with needle animation (`@keyframes pipboy-meter`), tune-meter and tune-wheel (rotated 45deg diamond), bottom panel with clips/switch, power button with flicker animation (`@keyframes pipboy-flicker`), roulette, top-right panel, spike-wheel with 3 rotated pseudo-elements, 5 decorative bumps. All classes namespaced `pipboy-` and scoped under `[data-theme='pipboy']`. Tab labels use `pipboy-lbl-*` prefix to avoid conflict with existing `pipboy-inv`/`pipboy-stat` component classes. Nav spans use `aria-label` + `role="tab"` for accessibility while CSS `::before` provides visible text. Wheel tab-names also clickable. Codepen decorative elements (supplies, info-bar with S3 icon images, HUD bar with HP/Level/AP) rendered in screen. Functional content area nested below nav using `.pipboy-screen-inner` absolute-fill flex column. 399/399 tests passing, build clean.
- **Pip-Boy Codepen exact CSS port (Wave 8)** (2026-04-05 15:55): Complete replacement of Pip-Boy CSS and layout with faithful Codepen port. Fixed 630×400 `.pip` container using `transform: scale()` for responsive scaling via `usePipBoyScale` hook (660×520 aspect ratio). Ported all Codepen CSS: pipfront (rounded corners/gradient), top panel ("Pip-Boy" label via `::after`), 5 screws (screw1-5 with rotation pseudo-elements), screen-border (370×290 rounded), inner screen (300×235 absolute), screen-reflection gradient (opacity 0.07), scan line animation (`@keyframes pipboy-scan`), nav tabs (CSS `::before` content labels), speakers (left/right with inset shadows), wheels (left 6° rotation + tab-names list clickable, right RADS/TUNE), RADS needle animation (`@keyframes pipboy-meter`), tune-wheel (45° rotated diamond), bottom panel (clips/switch/flicker power button `@keyframes pipboy-flicker`), spike-wheel (3 rotated pseudo-elements), 5 decorative bumps. Namespacing: all `pipboy-` prefix scoped to `[data-theme='pipboy']`, tab labels use `pipboy-lbl-*` to avoid collision with component classes. Accessibility: nav spans with `aria-label` + `role="tab"`, wheel tab-names clickable. Hardware details: all screws, speakers, RADS meter, tune wheel, power button, scan line, screen reflection faithfully ported. Tab switching preserves xterm state (CSS display:none). Responsive: side panels hide ≤768px. 399/399 tests passing, build clean, ready for Brady's Pip-Boy transition logic directive (phosphor persistence, scanline sweep, static burst, RADS needle feedback, power light pulsing).
- **Three theme-specific button fixes** (2026-04-06): (1) CRT toggle in StatusBar now conditionally rendered — only shows for themes with `crtEnabled: true` AND `id !== 'pipboy'` (apple2e, c64, ibm3270). Pip-Boy has its own built-in CRT system (scanlines, scan animation, screen reflection in Codepen CSS), Win95/LCARS have `crtEnabled: false`. (2) Win95 desktop icons now always visible on the teal desktop regardless of window state. Icons wrapped in `.win95-desktop-icons` container with `z-index: 1`, window has `z-index: 2` so it layers above. When maximized, icons are behind the window (correct Win95 behavior). (3) Pip-Boy Audio/Theme buttons styled as STIMPAK-style pills — `.pipboy-statusbar` repositioned to `bottom: 18px; right: 8px` to align with supplies bar. Generic statusbar chrome (indicator, spacer, border) hidden inside Pip-Boy. Buttons get `background: #78e461; color: #000; font-weight: 700; font-size: 5pt` matching the Stimpak(0)/Radaway(0) pills. Tests updated: Win95 desktop icon tests no longer require closing window first; accessibility tests unchanged (default apple2e theme still shows CRT toggle). 390/390 tests passing, build clean.
- **Pip-Boy text overlap + Vault Boy GIF + phosphor glow fix** (2026-04-05): Three-part fix for post-Codepen-port issues. (1) Text overlap: reduced nav tab font-size from 9pt→7pt and line-height from 31px→22px, shrunk supplies/HUD/info bars (font 5pt, tighter positioning), added overflow:hidden on nav and screen, sized screen-inner to stop at bottom 22px so content doesn't bleed behind HUD bars, added explicit stat row styles (pipboy-stat-row flex layout with overflow/ellipsis). (2) Walking Vault Boy GIF: downloaded `vaultboy2.gif` to `public/images/`, replaced SVG VaultBoy component with Codepen's original walking GIF div (`.pipboy-vaultboy` with 6 health bars `.pipboy-bar1`–`.pipboy-bar6`), centered in STAT tab with column layout, removed unused VaultBoy SVG import and limbHealth computation. (3) Phosphor glow: added `filter: drop-shadow(0 0 8px rgba(27,255,128,0.7)) brightness(1.2)` to vault boy, `box-shadow: 0 0 4px rgba(27,255,128,0.5)` to health bars. (4) Connection-aware state: added `.pipboy-vaultboy-thinking` class with pulsing green glow animation (`@keyframes pipboy-vaultboy-pulse` — brightness ramps 1.2→1.5 and drop-shadow intensifies), driven by `connectionStore.thinking`. Z-index layering standardized inside screen: content z:10, reflection z:90, scanline z:95, sweep z:100, static z:101. `prefers-reduced-motion` updated to disable vault boy pulse. 399/399 tests passing, build clean.
- **3026-04-07 (Wave 4 gap closure — GAP-2: CSS extraction):** Kare completed TelemetryDrawer gap closure. (1) GAP-2 CODE QUALITY — Extracted 3 inline `style={}` blocks from TelemetryDrawer.tsx to semantic CSS classes: `.telemetry-section-header` (section header container), `.telemetry-section-title--flush` (title with flush margin, used on agent roster + metrics sections), `.telemetry-timestamp` (fetch timestamp text styling). All classes added to `src/styles/telemetry.css` scoped to `.telemetry-drawer` namespace. Verification: build clean (0 TS errors, 0 lint warnings), 509 tests pass (no new failures), visual regression zero (CSS styles identical to inline values), all 5 themes display drawer correctly. TelemetryDrawer now feature-complete and production-ready.
- **Pip-Boy Wave 9 — Text Overlap + Vault Boy + Phosphor Glow** (2026-04-05 16:28): Complete resolution of post-Codepen-port text overlapping issues. Reduced nav tab font-size (9pt→7pt) and line-height (31px→22px) to eliminate button text wrapping. Shrunk supplies/HUD/info-bar fonts to 5pt with tightened positioning. Added `overflow: hidden` to nav and screen containers with explicit screen-inner height stop 22px before bottom edge to prevent content bleed behind HUD bars. Implemented `.pipboy-stat-row` flex layout with `overflow: hidden` and `text-overflow: ellipsis` for all data rows. Walking Vault Boy GIF integration: downloaded Codepen's `vaultboy2.gif` to `public/images/`, replaced SVG component with authentic Codepen HTML structure (walking GIF + 6 health bars `.pipboy-bar1`–`.pipboy-bar6`), centered in STAT tab with column layout. Phosphor glow effects: applied `filter: drop-shadow(0 0 8px rgba(27,255,128,0.7)) brightness(1.2)` to Vault Boy, `box-shadow: 0 0 4px rgba(27,255,128,0.5)` to health bars. Connection-aware thinking pulse: added `.pipboy-vaultboy-thinking` class with `@keyframes pipboy-vaultboy-pulse` animation (brightness 1.2→1.5, drop-shadow intensifies), driven by `connectionStore.thinking` boolean (set by ConnectionManager). Z-index standardization inside screen: content 10, reflection 90, scanline 95, sweep 100, static 101 (ensures Vault Boy always visible behind scanline). Updated `prefers-reduced-motion` to disable pulse animation. All text now readable within bezel, no overlapping. 399/399 tests passing, build clean, production-ready.
- **Controls consolidation to StatusBar** (2026-04-05): Eliminated duplicate control buttons. Removed the upper-right `controls` toolbar (MechanicalSwitch + AudioToggle + ThemeToggle) from App.tsx. Removed `header` prop from all 4 layout components (FullscreenLayout, Win95Layout, LcarsLayout, PipBoyLayout). StatusBar is now the single source of truth for all controls: CRT toggle (with `crt_toggle` sound effect via `useAudio`), Audio toggle (wired to `useAudio().toggleMute` — real localStorage-backed mute state, not the dead `connectionStore.audioEnabled` flag), and ThemeToggle. All 6 themes verified to have `crt_toggle` sound definitions in SKIN_PROFILES. Updated `/status` command to read audio mute from localStorage instead of Zustand. AudioToggle and MechanicalSwitch standalone test files marked `describe.skip` (components still exist but are no longer rendered). 380/380 tests passing + 15 skipped, build clean.
- **Pip-Boy STAT screen Codepen fidelity fix** (2026-04-05): Three changes to match Codepen reference screenshot exactly. (1) Default tab changed from DATA→STAT: `usePipBoyTransition('STAT')` in PipBoyLayout so Pip-Boy always loads on STAT page first showing Vault Boy + S.P.E.C.I.A.L. stats. (2) Info-bar icon replacement: replaced all 6 S3-hosted PNG URLs (pipboy-gun2, aim2, helmet2, shield2, voltage2, nuclear2) with inline SVG data URIs in green (#64d515) — gun silhouette, crosshair with center dot, helmet profile, shield outline, lightning bolt, trefoil radiation symbol. Each icon sized to match Codepen proportions (23px weapon/helmet, 14px aim/shield/voltage/nuclear). (3) Supplies bar name: changed "UPLINK" to "BRIAN" per owner request. Sub-tab bar (STATUS/SPECIAL), supplies bar (Stimpak/Radaway/name), info-bar with 6 icons + numbers, and HUD bar (HP 365/365, Level 48 with progress bar, AP 110/110) were already present from Wave 8 Codepen port — all CSS positioning and styling verified matching Codepen. Updated 3 PipBoyLayout tests to reflect STAT-first default (default tab assertion, tab switching test, active class test, terminal preservation tests updated for STAT-as-initial). 380/380 tests passing + 15 skipped, build clean.
- **Win95 theme fixes** (2026-04-05): Three-part fix for Win95 theme issues. (1) Theme switching restored: StatusBar (containing ThemeToggle, CRT toggle, Audio toggle) was missing from Win95 layout after controls consolidation — re-added by passing `statusBar` prop to Win95Layout and rendering it inside `.win95-statusbar` with CSS hiding the duplicate connection indicator. ThemeToggle now visible and clickable inside the Win95 window statusbar. (2) Theme switcher desktop icon: added 🎨 "Display Properties" icon on the teal desktop when window is closed, positioned below the SQUAD UPLINK icon (`top: 110px`). Single-click selects (mutual exclusion with app icon), double-click or Enter cycles to next theme. (3) Maximize fullscreen fix: maximize/restore/taskbar-restore now dispatch `window.resize` event via `requestAnimationFrame` so xterm.js FitAddon re-fits the terminal to the new container size. CSS `.win95-window--maximized` already removes max-width/border constraints. 9 new tests added (5 theme icon, 3 statusbar theme toggle, 1 maximize resize). 389/389 tests passing + 15 skipped, build clean.
- **Pip-Boy STAT screen refinements** (2026-04-06): Five UI fixes matching Codepen reference screenshots. (1) Tab underlines — no gap: Nav bar CSS changed to `display: flex; align-items: flex-end;` with span `line-height: 1` and `padding-bottom: 3px`, so tab text sits flush against the bottom border with no visible gap. (2) Removed STATUS/SPECIAL from nav: Deleted the `<p>` element containing `.pipboy-lbl-status` and `.pipboy-lbl-special` spans — top tab row now shows only STAT, INV, DATA, MAP, RADIO. (3) Connection status in upper right: Added `.pipboy-connection-status` span in the nav bar after the 5 tabs, styled with `margin-left: auto` to push it right. Shows CONNECTED (green), CONNECTING... (amber), or DISCONNECTED (red) using `connectionStore.status`. (4) Icons below Vault Boy: Added the weapon/aim/helmet/shield/voltage/nuclear icon row with numbers inside `PipBoyStat.tsx` below the Vault Boy, using `.pipboy-info-bar-inline` class (relative positioning, flex centered). (5) S.P.E.C.I.A.L. to the right: Changed `.pipboy-stat-data-col` to `align-self: flex-end; text-align: right;` and `.pipboy-stat-list` to `display: inline-block; text-align: left;` so the S.P.E.C.I.A.L. section sits on the right side, no longer overlapping Stimpak/Radaway. Removed unused `STATUS_COLOR`, `STATUS_LABEL` constants and `status` selector from PipBoyStat (status now shown in nav). 418/418 tests passing + 15 skipped, build clean.



- **CRT toggle + Win95 icons + Pip-Boy pill buttons** (2026-04-06 02:20): Three focused refinements to complete theme UI polish. (1) CRT toggle conditional rendering: StatusBar now only renders CRT toggle for themes where \crtEnabled: true\ (Apple2e, C64, IBM3270). Win95 and LCARS have \crtEnabled: false\, Pip-Boy has built-in CRT system (scanlines + scan animation from Codepen CSS) — toggle hidden for all three. Declutters interfaces. Conditional in EffectsPanel: \const showCRT = theme.crtEnabled && id !== 'pipboy';\. (2) Win95 desktop icons always visible: desktop folder icon (and 'Display Properties' theme switcher icon when window closed) now have persistent \z-index: 1\ wrapped in \.win95-desktop-icons\ container. Window has \z-index: 2\, layering correctly — icons visible when window minimized/closed, behind window when maximized. Matches authentic Win95 behavior. (3) Pip-Boy Audio/Theme buttons styled as green STIMPAK-style pills: \.pipboy-statusbar\ repositioned \ottom: 18px; right: 8px\ to align with supplies bar. Generic statusbar chrome (indicator, spacer, border) hidden inside Pip-Boy via CSS \display: none\. Buttons receive STIMPAK styling: \ackground: #78e461; color: #000; font-weight: 700; font-size: 5pt;\ green accent matching Stimpak(0) and Radaway(0) pill graphics in supply bar. Rounded corners via \order-radius: 4px\, internal padding for clickability. 390/390 tests passing, build clean. Three themes now have UI-specific control styling: CRT-era (Apple2e/C64/IBM3270) show toggle, Win95 has traditional statusbar text, Pip-Boy has vault-tech green pills.
- **Apple IIe 3D theme refinement** (2026-04-06): Five-part rework of Apple IIe 3D layout per Brian's feedback. (1) Terminal fills full monitor: moved `.a2e-monitor__terminal` out of `.a2e-monitor__screen-2` in JSX, positioned absolutely to fill the `.a2e-monitor__shadow` dark area (61.15x47 vmin). Black background, 0.75vmin border-radius, green text (#33ff33). Screen layers still create phosphor edge glow beneath. (2) Scene centering: `perspective-origin` changed from `70% 40%` to `50% 42%`, monitor repositioned from `left:31/top:11.5` to `left:12/top:6`. (3) Larger monitor, smaller keyboard: monitor scaled from 66x47.25 to 76x54 vmin (all internal elements proportionally scaled ~1.15x). Keyboard scaled via `transform: scale(0.65)` - decorative but compact. (4) CRT scanlines scoped to terminal only: removed `<CRTOverlay>` from Apple2eLayout.tsx (import removed, `crt-screen` class removed). Added `::after` pseudo-element on `.a2e-monitor__terminal` with repeating-linear-gradient scanlines (2px pitch, rgba(0,0,0,0.12)) and inner phosphor vignette (box-shadow inset green glow + dark edges). Top/bottom shadow overlays get `z-index:11` to render curved screen edge effect above terminal. (5) Floppy/shadow repositioning: monitor-shadow, back-shadow, Disk II all shifted to match new layout center. Title text reduced from 30vmin to 20vmin, repositioned. TypeScript clean, Vite build clean.
- **Apple IIe 3D visual fixes from screenshot** (2026-04-06): Four-part fix from Brian's screenshot feedback. (1) Screen overlay removal: `.a2e-monitor__screen` and `.a2e-monitor__screen-2` (including `.a2e-top-shadow` and `.a2e-bottom-shadow` variants) set to `display: none` — these decorative elliptical/gradient layers from the original Codepen were rendering curved dark bars over the xterm terminal text. Terminal is now the topmost visible layer inside the monitor. (2) Larger monitor + keyboard pushed to bottom: monitor enlarged from 76×54 to 82×58 vmin (`left:9, top:4`), keyboard pushed from `top:63` to `top:72` with scale reduced from 0.65 to 0.55, shadow positions updated to match. (3) Disk II full-click: moved `onClick`, `role="button"`, `aria-label`, `tabIndex`, `onKeyDown`, and `cursor:pointer` from `.a2e-floppy-slot-container` to parent `.a2e-floppy` div so clicking anywhere on the drive triggers the sound. (4) LED disk activity indicator: added `diskActive` state with `useRef` timeout (5s duration), LED gets `a2e-floppy-led--active` class when either `isConnected` OR `diskActive` is true, timeout cleanup on unmount. TypeScript clean, Vite build clean.
- **Apple IIe monitor bezel restoration + layout maximization** (2026-04-06): Full bezel chrome restoration matching Codepen reference. (1) Re-enabled `.a2e-monitor__screen` (green glass #4e654a, z-index:3) and `.a2e-monitor__screen-2` (gradient overlay z-index:4) — previously `display:none`. Added `.a2e-top-shadow` and `.a2e-bottom-shadow` variants at z-index:15 with `pointer-events:none` for CRT curvature illusion ON TOP of terminal. (2) Z-index layering: soft-shadow/shadow z:1, inner/inner-shadow/light/dark z:2, screen z:3, screen-2 z:4, terminal z:10, top/bottom shadows z:15, scanlines z:20, logo/power-switch z:20. Green glass peeks around terminal edges. (3) Terminal repositioned INSIDE screen bezel: `top:7.2vmin, left:7.8vmin, width:53vmin, height:39vmin` (was `top:5.5, left:5.5, width:calc(100%-11vmin), height:calc(100%-13vmin)` — too large, covered all bezel chrome). (4) Layout: keyboard pushed to `top:74vmin, scale(0.5)` — small and decorative at bottom. Floppy repositioned to `top:25vmin, left:82vmin, scale(0.7)` — right of monitor, vertically centered. (5) Title text made subtle: font-size 20→14vmin, opacity 0.35. Line element z-index:5. TypeScript clean, Vite build clean.
- **C64 Codepen port — complete 3D rework** (2026-04-06): Full replacement of C64 layout from `fullscreen` mode to dedicated `c64` layout mode, following the Apple IIe pattern. Created 4 new files: (1) `src/styles/c64-3d.css` — all CSS with `c64-` prefix scoped under `[data-theme='c64']`. Dark #1a1a1a background, CRT monitor housing (#3f3b37 body, #2a2a2a border, 20px border-radius, deep box-shadow), blue screen (#4040ff with #8888ff light-blue border), CRT scanlines via `::after` on `.c64-terminal` (repeating-linear-gradient, pointer-events:none, z-index:20), beige keyboard (#beae7c→#d4c48e gradient) with perspective tilt (rotateX(20deg) translateZ(40px)), dark gray keys (#4a4a4a→#3a4a3a) with orange (#ffa500) shift labels via `data-shift` attr `::after`, brown function keys (#854f1b→#6a3f15), floating transparent statusbar at bottom, responsive media query for ≤600px. (2) `src/components/C64/C64Keyboard.tsx` — authentic C64 layout with 4 main rows + space row + F-keys column on right. Row 1: ← 1-0 + - £ HOME INST. Row 2: CTRL QWERTYUIOP @ * ↑ RESTORE. Row 3: RUN/STOP ASDFGHJKL : ; = RETURN. Row 4: C= SHIFT_LOCK ZXCVBNM , . / SHIFT. Space row. F-keys: F1/HELP F3/LIST F5/LOAD F7/SAVE. (3) `src/components/C64/C64Layout.tsx` — React component with container→monitor→screen→terminal structure, keyboard below, floating statusbar. Props: children, statusBar, crtEnabled. No pulsating title. (4) `src/components/C64/index.ts` — barrel export. Modified: c64.ts layout `fullscreen`→`c64`, index.ts added `'c64'` to layout union type, App.tsx added C64Layout import + c64-3d.css import + `if (layout === 'c64')` branch. CRT effects scoped to terminal `::after` (no global CRTOverlay). TypeScript clean, Vite build clean.
- **Unified controls + fullscreen terminal** (2026-04-06): Three-part feature delivering consistent UI controls across all 9 themes. (1) Floating control bar: redesigned StatusBar as `.uplink-controls` — fixed bottom-right, semi-transparent dark background with backdrop-blur, compact icon buttons for theme cycle (emoji per theme), audio toggle (🔊/🔇), CRT toggle (📺, conditional for apple2e/c64/ibm3270), connection status dot (green/yellow/red), and fullscreen toggle (⛶/⊟). Uses `role="toolbar"` with `aria-label="Uplink controls"`. Consistent across all themes except Pip-Boy (hidden via `[data-theme='pipboy'] .uplink-controls { display: none }`). (2) Terminal fullscreen mode: added `terminalFullscreen: boolean` and `toggleFullscreen()` to connectionStore. When active for chrome themes (apple2e, c64, win95, lcars, pipboy), skips layout wrapper and renders terminal at 100vw×100vh via `.terminal-fullscreen` class. Escape key exits fullscreen. State persists through theme switches. (3) Vertical fill fixes for fullscreen themes: added `100vh` flex column layouts for `.fullscreen-layout` scoped to ibm3270, wopr, muthur, matrix via global.css. WOPR `.terminal-container` gets `flex:1; min-height:0` for proper fill. MU-TH-UR keeps 60vw content centering but fills vertically. Layout refactor: removed statusBar prop from Win95/Apple2e/C64/LCARS layouts (controls now floating only), PipBoy still receives prop but floating bar hidden by CSS. Updated accessibility tests (tab order, toolbar role, emoji text content, connection dot aria-labels). Updated Win95 tests (ThemeToggle now in floating bar, not win95-statusbar). 418/418 tests passing (2 new fullscreen toggle tests), build clean.
- **Wave 6 M7 — Font optimization + accessibility hardening** (2026-04-06): Six-part font/a11y sweep across all 9 themes. (1) CSP font fix: removed `@import url('https://fonts.googleapis.com/...')` from muthur.css (Share Tech Mono) and wopr.css (VT323) — both fonts already declared locally in fonts.css with `font-display: swap`. External imports violated CSP `font-src 'self'`. (2) Global font-family declarations: added `[data-theme='wopr']`, `[data-theme='muthur']`, and `[data-theme='matrix']` body font-family rules to global.css — were missing for the 3 newest themes. (3) Skip-to-content link: added `<a href="#terminal" class="skip-link">Skip to terminal</a>` to all 7 layout return paths in App.tsx. Terminal container gets `id="terminal"` anchor target. CSS `.skip-link` already existed in accessibility.css. (4) Focus indicators for 4 newer themes: added `:focus-visible` rules for pipboy (#1bff80 phosphor green), wopr (#7cb4fc cyan), muthur (#7af042 green), matrix (#00ff00 bright green) — each with matching theme-colored glow box-shadow. (5) Reduced motion coverage: added `prefers-reduced-motion` rules for Matrix rain canvas (opacity:0, no transition), MU-TH-UR glow pulse (animation:none), W.O.P.R. scan sweep (animation:none), Pip-Boy scan/meter/flicker/vault-boy-pulse (all animation:none). (6) All 8 @font-face declarations confirmed with `font-display: swap`, PrintChar21 preloaded in index.html. 436/436 tests passing (+18 from skip-link detection in existing test suites), TypeScript clean, build clean.

- **2026-04-07 (Wave 4–6 batch completion):** Cross-agent update from Jobs, Woz, and Hertzfeld. (1) Jobs' Wave 4 TelemetryDrawer gap analysis delivered: 3 gaps identified (StatusBar button CRITICAL, inline styles CODE QUALITY, focus management ACCESSIBILITY). Component is ~90% complete; gaps assigned for implementation (Woz: button + focus management, Kare: CSS classes). (2) Woz's Wave 5 (M6) SWA deployment: CI workflows enhanced (PR staging enabled, `close_staging` job added), SWA config validated complete (font caching, CSP, fonts excluded from SPA fallback). 418 tests passing. (3) Hertzfeld's Wave 6 test expansion: 91 new tests (accessibility 15, usePipBoyTransition 21, utilities 25, connectionStore 30). Suite now 509 total passing, 15 skipped. (4) Woz's audio scaffolding utility: `public/audio/README.md` updated (9 themes, 12 sounds), theme placeholder folders created with .gitkeep, `muthur/` naming standardized (corrected from task spec "mothur" to match codebase ThemeId). Orchestration logs written (5 per-agent + session summary). Decision inbox merged into decisions.md (6 new decision entries: Jobs Wave 4 gaps, Kare Apple2e overlays + CRT scoping + C64 rework + CSP fonts, Woz audio scaffolding). All inbox files deleted. Next steps: close Wave 4 gaps (Woz: StatusBar button + focus mgmt, Kare: inline CSS classes), commit .squad/ changes.
