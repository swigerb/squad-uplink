# Project Context

- **Owner:** Brady
- **Project:** squad-uplink — A TypeScript/React/Vite front-end web app with a retro Apple IIe and Commodore 64 theme to control Squad agents remotely using the squad-rc feature and devtunnel. Will be hosted in Azure Static Web Apps.
- **Stack:** TypeScript, React, Vite, Azure Static Web Apps, devtunnel
- **Created:** 2026-04-05

## Core Context

Tester for squad-uplink. Responsible for test suite architecture, unit/component/integration testing with Vitest and React Testing Library, edge case identification, and quality gates.

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

- **2026-04-05:** Created comprehensive test strategy (`test-strategy.md`) covering 60+ test cases across unit, component, integration, and edge-case categories. Key decisions: Vitest+jsdom, co-located test files, custom WebSocket/xterm.js/Web Audio mocks, behavior testing over snapshots, 80% coverage floor (95% for hooks). Decision submitted to inbox for team review.
- **2026-04-05:** Protocol constraints that need heavy edge-case coverage: 20 msg/min WS rate limit, 500-message replay buffer, 4-hour session TTL, one-time ticket auth. These are the production fire starters.
- **2026-04-05:** C64 40-column mode is a testing concern — need to verify line wrapping/truncation behavior when switching from 80-col Apple IIe to 40-col C64 mid-session.
- **2026-04-05 (Cross-Agent):** Scaffold ready for test implementation. Vitest config included (`vitest.config.ts`), path alias `@/` synchronized between vite and tsconfig, structure supports co-located tests. Woz's 74 files deliver foundation for 60+ test cases. Start writing tests against M0 scaffold immediately.
- **2026-04-05:** Implemented test suite — 151 tests across 6 test files (3 hooks, 2 components, 1 theme config). All passing. MockWebSocket and MockAudioContext utilities created in `src/__mocks__/`. Key finding: WebSocket `connect()` resets `retriesRef` to 0 on every call including reconnect — this means exponential backoff delay never actually escalates and maxRetries is never reached through the reconnect timer path. Filed as a known behavior issue for Woz.
- **2026-04-05:** Kare expanded themes from 2→5 mid-session. Tests needed immediate adjustment: toggle cycle is now 5-step, title text changed, labels record expanded. localStorage pollution between test files was a real issue — every component test file wrapping in ThemeProvider must `localStorage.clear()` in `beforeEach`. jsdom doesn't reliably convert hex colors in inline styles to rgb — use structural assertions (element presence) over computed style checks.
- **Wave 2 completion** (2026-04-05): All 151 tests passing. Comprehensive coverage of hooks, components, and theme configs. Critical bug identified: WebSocket reconnect backoff never escalates (connect() resets retry counter on every call). Bug assigned to Woz for M1. Test suite is stable and effective — caught real production risk. localStorage clearing pattern established, jsdom color conversion workaround documented.
- **Wave 3 completion** (2026-04-05): 233 tests across 10 files, all passing. Woz and Kare delivered ConnectionManager class, command parser, MechanicalSwitch, AudioToggle, and expanded useAudio (11 sound types with SoundSequence multi-step support). Key findings: (1) Woz correctly fixed the backoff bug — `connect()` no longer resets retries, only `onopen` does; `connectFresh()` added for user-initiated connects. (2) Audio expanded from 5→11 types with mute/toggleMute and localStorage persistence. (3) MechanicalSwitch uses props `{crtEnabled, onToggle}` not store — three visual variants (lever/checkbox/pill) per skin. (4) AudioToggle takes `{muted, onToggle}` props with useTheme styling. (5) Component tests live in `src/components/__tests__/` — import paths are `../ComponentDir/Component`, not `../../`. (6) ConnectionManager rate limiting uses threshold (16) + hard cap (20) with drain timer, not simple counter. (7) Commands module uses singleton `connectionManager` + `useConnectionStore.getState()` — no context param.
- **Wave 4+6 tests** (2026-04-05): 314 tests across 13 files (293 passing, 21 skipped). Added 3 new test files: (1) `src/components/__tests__/TelemetryDrawer.test.tsx` — 21 tests in 5 `describe.skip` blocks covering rendering, connection metrics, session info, /status endpoint, and edge cases. All skipped pending Woz's implementation. Tests cover: keyboard shortcut open/close, latency/msg-per-sec/uptime display, masked session token, auto-refresh with 30s interval, graceful error handling, rapid toggle resilience. (2) `src/styles/__tests__/fonts.test.ts` — 32 tests verifying @font-face declarations (font-display:swap on all 6 rules), theme fallback chains (each theme's primary+fallback+generic stack), and graceful degradation (custom font names match CSS declarations). Key finding: font stacks changed from original spec — Apple IIe uses "Apple II" fallback not "Courier New", C64 uses "PetMe", IBM uses "IBM Plex Mono", Win95 uses "Fixedsys"/"Courier New", LCARS uses "Antonio". (3) `src/components/__tests__/accessibility.test.tsx` — 28 tests covering keyboard navigation (Tab focus + Enter/Space activation for ThemeToggle, MechanicalSwitch, AudioToggle, StatusBar), ARIA attributes (aria-label, aria-pressed, aria-hidden, title attributes), and reduced motion (CRT flicker scoping, CRTOverlay conditional rendering, matchMedia mock for prefers-reduced-motion). Key pattern: Zustand store updates in tests must be wrapped in `act()` to avoid React warnings.
- **2026-04-05 (Wave 4 cross-agent update from Woz):** TelemetryDrawer component shipped — modern dark glass panel that intentionally breaks retro aesthetic. Metrics: latency, msg/s (rolling 10s window), uptime, reconnect count. Session info: tunnel URL, masked token. Agent roster + raw JSON /status with syntax coloring. Auto-refresh 30s when open. Keyboard: Ctrl+Shift+T, Escape/backdrop close. Zustand `connectionStore` extended with `telemetry` object and actions. Render as App sibling (z-index 9999) — works all 5 themes. 283 tests passing.
- **Test compilation fix** (2026-04-05): Fixed two build-breaking issues and un-skipped TelemetryDrawer tests. (1) `fonts.test.ts` — replaced `node:fs`/`node:path`/`__dirname` with Vite `?raw` import (`import fontsCSS from '../fonts.css?raw'`); fixed regex match destructuring to use `match[1]` instead of `([, body])`. (2) `TelemetryDrawer.test.tsx` — rewrote all 5 `describe.skip` blocks as active tests against Woz's actual implementation. Key differences from original spec: drawer is always in DOM (CSS transform show/hide, not conditional render); Ctrl+Shift+T lives in App.tsx not the component; open/close tested via Zustand `setDrawerOpen()`/`toggleDrawer()`; Escape key handler is on `window` not `document`; metrics use `telemetry.latencyMs`/`inboundMps`/`outboundMps` sub-object not top-level fields; /status uses `connectionManager.fetchStatus()` (mocked) not direct `fetch`. Mocked `@/lib/ConnectionManager` module to isolate component tests. Suite now 319 tests, 0 skipped, 0 failures. Build passes clean.
- **Wave 5 completion** (2026-04-05): All 6 milestones shipped. Test suite final: 319 passing, 0 skipped, 0 failures. Comprehensive coverage across hooks (useWebSocket, useTheme, useAudio), components (Terminal, ConnectionStatus, StatusBar, ThemeToggle, MechanicalSwitch, AudioToggle, TelemetryDrawer), integration (ConnectionManager, command parser, theme switching mid-session), accessibility (keyboard nav, ARIA, reduced motion), and performance (fonts, fonts.css, bundle metrics). Edge cases: C64 40-col mode line wrapping, WebSocket rate limiting protocol (20 msg/min cap), 500-message buffer, 4-hour TTL. Bug tracked: backoff escalation was fixed in Wave 3, confirmed in Wave 5. All production fire starters have test coverage. Quality gate: 80% coverage floor (95% for hooks) maintained. Ready for production deployment.
- **2026-04-05 (Wave 6 cross-agent update from Kare):** Font optimization + accessibility audit delivered. @font-face moved to `public/fonts/`, use `font-display: swap`. Theme-specific fallback chains: apple2e→Apple II, c64→PetMe, ibm3270→IBM Plex Mono, win95→Fixedsys→Courier New, lcars→Antonio. Focus indicators per theme (phosphor glow CRT, dotted outline Win95, orange LCARS). Reduced motion media query disables animations (flicker, glow transitions, switch slides). ARIA: aria-label, aria-pressed, aria-live, role="status", role="application". Keyboard nav verified. StatusBar has `aria-live="polite"`. Terminal has `role="application"`. 293 tests passing.
- **File-based audio tests** (2026-04-05): Wrote 15 spec-driven tests for Kare's hybrid audio rework across 5 describe blocks: audio file loading (5 tests), AudioBufferCache (3 tests), hybrid playback (4 tests), and manifest (3 tests) — all in `describe.skip` pending Kare's implementation. Also added 4 active backward-compatibility tests that verify the existing API contract (`{ play, muted, toggleMute }`, localStorage persistence, procedural-only fallback, mute toggle for both paths). Updated `src/__mocks__/audio.ts` with `MockAudioBuffer`, `MockAudioBufferSourceNode`, `decodeAudioData` mock, `createBufferSource` mock, and `createMockAudioFetch()` helper for simulating fetch success/failure/network-error. Suite now 338 total (323 passing, 15 skipped). All 13 test files green. Key pattern: mock fetch uses URL substring matching ('missing'/'fail' → 404, 'network-error' → TypeError, everything else → 200 with ArrayBuffer). Tests designed to un-skip directly once Kare lands the hybrid `useAudio`.
- **2026-04-05 (Wave audio tests — implementation complete):** Delivered comprehensive test suite for hybrid audio system. Brady's request for real audio files in sample form prompted rework from procedural-only to hybrid. Spec-driven 15 tests: file loading (5) covering `.mp3`/`.wav`/`.ogg` decode via Web Audio API, network error handling, missing file fallback; AudioBufferCache (3) covering singleton pattern, per-skin preloading, concurrent fetch handling; hybrid playback (4) covering cached vs procedural logic, non-blocking behavior, backward compatibility; manifest (3) covering sound→path mapping, directory structure, missing entry handling. All 15 in `describe.skip` pending audio files. Backward-compat tests (4) active and passing: verify `{ play, muted, toggleMute }` API, localStorage persistence, procedural-only fallback, mute toggle for both paths. Mock updates: `MockAudioBuffer`, `MockAudioBufferSourceNode`, `decodeAudioData`, `createMockAudioFetch()` with URL-based fetch simulation (substring matching: 'missing'/'fail'→404, 'network-error'→TypeError, else→200). Suite now 338 total (323 passing, 15 skipped). Un-skip trigger: source real audio files, populate `public/audio/{skinId}/`, all 15 tests will execute automatically.
- **2026-04-05 (Wave 6 Pip-Boy theme tests — implementation complete):** Delivered comprehensive test suite for Pip-Boy 3000 theme. Kare implemented full Pip-Boy layout with 5-tab navigation (STAT/INV/DATA/MAP/RADIO), green-on-black CRT, custom flicker, phosphor effects, boot warm-up glow, VT323/Share Tech Mono fonts, and audio profile. Woz built 5 data-driven tab components with Zustand store extensions. Hertzfeld delivered 25 spec-driven tests in `src/components/__tests__/PipBoyLayout.test.tsx` across 7 describe blocks, all un-skipped and passing. Coverage: (1) Theme integration (6 tests) — color palette, font stack, tab styling, persistent data layout; (2) Tab navigation (4 tests) — click, keyboard (arrow keys/Tab), focus management, default tab (DATA); (3) Terminal preservation (3 tests) — xterm mounted once, CSS display:none for non-DATA tabs, scroll position preserved across tab switches; (4) Data components (5 tests) — PipBoyStat SPECIAL bars, PipBoyInv inventory, PipBoyData message history, PipBoyMap topology tree, PipBoyRadio command console; (5) CRT effects (3 tests) — custom pipboy-flicker animation, scanline overlay, phosphor glow backdrop-filter; (6) Boot sequence (2 tests) — startup messages, audio trigger (sine sweep); (7) Audio (2 tests) — chirp on message, click on state change. All tests passing. Build clean. 374/374 tests passing, 0 skipped, 0 failures. Pip-Boy theme and components production-ready.
- **2026-04-05 (Wave 7 — Pip-Boy full device chrome integration):** Wave 7 completion. Hertzfeld un-skipped all 25 Pip-Boy tests (7 describe blocks), converting from `describe.skip` to active tests. Tests verify: device chrome rendering (tan metal, grid layout, inset shadows), screen bezel CRT well, side panel visibility/responsive hiding, decorative elements (screws, label, power button), VaultBoy component and limb health bars, idle animations, terminal preserved on tab switches, all data components render correctly with Zustand integration, graceful disconnected states ("NO SIGNAL"), CRT effects (flicker, scanline, phosphor), boot sequence, audio integration. All 25 tests now passing. Full integration verified: Kare's device chrome (CSS grid 3-column, decorative metal aesthetics, VaultBoy) + Woz's data components + Hertzfeld's comprehensive test coverage. 399/399 tests passing, 0 skipped, 0 failures. Pip-Boy 6th skin production-ready with all other skins.
- **2026-04-05 (Consolidated controls — test update):** Brady directed all controls (CRT toggle, Audio toggle, ThemeToggle) moved to StatusBar. Kare landed the implementation. Hertzfeld updated tests: (1) Rewrote `accessibility.test.tsx` — removed standalone MechanicalSwitch/AudioToggle keyboard nav and ARIA tests; all control tests now target StatusBar surface (CRT toggle, Audio toggle, ThemeToggle all tested within StatusBar). Added assertions: ThemeToggle rendered inside StatusBar, no `role="toolbar"` exists, CRT/Audio/ThemeToggle keyboard activation (Enter+Space), CRT aria-pressed state, ThemeToggle title updates. (2) Skipped `MechanicalSwitch.test.tsx` (7 tests) — component deprecated, no longer rendered in App. (3) Skipped `AudioToggle.test.tsx` (8 tests) — component deprecated, no longer rendered in App. (4) Win95Layout toolbar test already updated by Kare. Key finding: Kare's StatusBar now uses `useAudio().toggleMute` (not `connectionStore.toggleAudio`) and CRT button calls `play('crt_toggle')` before toggling. Suite: 380 passing, 15 skipped, 0 failures across 15 test files.
- **2026-04-06 (Pip-Boy STAT screen — implementation complete):** Kare implemented Pip-Boy STAT tab as default landing screen for PipBoyLayout. Replaced all S3 PNG icons with inline SVG data URIs (6 icons: S.P.E.C.I.A.L. attributes, embedded as base64 SVG strings for fast load). Renamed "Supplies" display to "BRIAN" in info bar (matches Pip-Boy lore context). Verified: STAT tab renders as default on theme load (`usePipBoyTransition` hook), sub-tabs show S.P.E.C.I.A.L. bars mapped to connection metrics (latency→S, uptime→P, etc.), info bar displays player name + BRIAN count + XP/level, HUD bar shows AP counter and health/rads/limb status from connectionStore telemetry. SVG icons render correctly across all viewports. Reduced motion media query respected for animations. Test suite remains 380 passing, 15 skipped. Build clean — no TS errors, SVG data URIs optimized for gzip. Production-ready.
- **2026-04-06 (Wave 6 M7 part 3 — accessibility + coverage gaps):** Added 91 new tests across 3 new files + expanded accessibility.test.tsx. Suite: 509 passing, 15 skipped, 0 failures across 18 test files. (1) `accessibility.test.tsx` — expanded from 28→43 tests: added TelemetryDrawer focus management (drawer role/aria-label, close button aria-label, Escape closes drawer, backdrop click closes, keyboard focus), theme switching + keyboard nav preservation (buttons remain focusable after theme switch, CRT toggle visibility changes don't break tab order), fullscreen toggle aria updates, connection status aria-live/all-states coverage. Mocked ConnectionManager module for TelemetryDrawer isolation. (2) `usePipBoyTransition.test.ts` — 21 new tests: initial state (default DATA tab, custom tab, idle phase), PIPBOY_TABS constant, direct tab switching (activeTab immediate, no-op same tab, blocked during transition), transition phase timing (static→fade→sweep→idle at 0/50/200/400ms), nextTab/prevTab with wraparound, tabIndex accuracy, sequential cycling. Uses fake timers for deterministic phase testing. (3) `utilities.test.ts` — 25 new tests for formatters.ts + bootMessages.ts: formatAgentList (empty/single/multi/unknown status, ANSI codes, line endings), formatStatus (connected/disconnected, tunnel URL, line endings), getBootMessage (all 9 themes return content, /help instruction, theme-specific strings, LCARS stardate format, unknown theme fallback). (4) `connectionStore.test.ts` — 30 new tests for Zustand store: initial state (6), connection actions (4), toggle actions (6), telemetry actions (3), message history buffer cap at 50 (2), command history cap at 10 (2), Pip-Boy state (4), hardware feedback (2). Key patterns: TelemetryDrawer tests need `vi.mock('@/lib/ConnectionManager')` to avoid real module import; usePipBoyTransition tests need `vi.mock('@/hooks/useAudio')` and fake timers; connectionStore tests are pure state — no React rendering needed.

- **2026-04-07 (Wave 4–6 batch completion):** Cross-agent update summary from Jobs, Woz, and Kare. (1) Jobs' Wave 4 TelemetryDrawer gap analysis: 3 gaps identified (StatusBar button CRITICAL, inline styles CODE QUALITY, focus management ACCESSIBILITY). Component ~90% complete; gaps assigned (Woz: button + focus mgmt, Kare: CSS classes). (2) Woz's Wave 5 (M6) SWA deployment: CI workflows enhanced (PR staging enabled, `close_staging` job added), SWA config validated complete. 418 tests passing. (3) Kare's Wave 6 fonts/a11y: CSP-compliant font loading, accessibility audit, C64/Apple IIe theme cleanup. 436 tests passing. (4) Hertzfeld's Wave 6 test expansion delivered 91 new tests (accessibility 15, usePipBoyTransition 21, utilities 25, connectionStore 30). Suite now 509 total passing, 15 skipped, 0 failures. (5) Woz audio scaffolding utility: updated `public/audio/README.md`, created theme placeholder folders. (6) Orchestration logs written (5 per-agent + session summary). Decision inbox merged into decisions.md (6 new entries). All inbox files deleted. Next: close Wave 4 gaps.

- **2026-04-07 (Pip-Boy STAT v2 validation):** Hertzfeld verified Kare's two-round Pip-Boy STAT refinement batch. (1) Round 1 — Control consolidation + icon restructure: StatusBar is now sole control source (no duplicate `.pipboy-controls` inside PipBoyStat), 6 weapon/stat icons repositioned below Vault Boy in flex grid, MSGS/RECONNECTS telemetry removed from STAT surface (belongs in TelemetryDrawer), AP truncation fixed (width constrained, font-size 5pt), top nav bar width aligned to Codepen reference (630px base). (2) Round 2 — PNG asset replacement: 6 real PNG icons sourced (gun, aim, helmet, shield, voltage, nuclear) and stored in `public/images/pipboy/`, info-bar restructured to use `<img>` tags instead of Unicode symbols, responsive sizing (18–20px, scales ≤768px), hover opacity effects added. Quality: 509/509 tests passing (0 failures, 0 regressions), TypeScript clean, build clean. Responsive verified on mobile viewports. Orchestration log + session log written. Ready for team history merge and git commit.

- **2026-04-07 (WebSocket subprotocol auth tests):** Woz refactored ConnectionManager WebSocket auth from query-param tokens to subprotocol-based auth (`['squad-rc', 'access_token-<token>']`). Also added trailing slash stripping (`wsUrl.trim().replace(/\/+$/, '')`), close code diagnostics (logs `event.code`, `event.reason`, `event.wasClean`), and error event logging. Woz pre-updated MockWebSocket to capture `protocols` arg and fixed the first existing test. Hertzfeld added 13 new tests across 6 describe blocks: (1) subprotocol auth (3 tests — token as subprotocol, no-token case, no query param leakage); (2) trailing slash stripping (3 tests — single/multiple/none); (3) whitespace handling (3 tests — leading/trailing/combined with slashes); (4) ticket path with subprotocol (2 tests — ticket as query param + token as subprotocol, no-token ticket path); (5) close code diagnostics (3 tests — code/reason/wasClean logging, empty reason shows "(none)", clean vs unclean close); (6) error event logging (1 test — event object passed to console.error). Suite now 524 passing, 15 skipped, 0 failures across 16 test files. Key pattern: HTTP→ticket exchange path uses both mechanisms (ticket=query param, token=subprotocol), while direct WS path uses subprotocol only.


- **2026-04-08 (Cross-agent WebSocket auth tests — collaboration with Woz):** Wave completion. Woz and Hertzfeld delivered unified WebSocket Dev Tunnel auth fix. (1) Woz's 3 code fixes in ConnectionManager.ts: subprotocol auth (replaces query param with `access_token-<JWT>` subprotocol), trailing-slash stripping on tunnel URLs, close-code diagnostics (`event.code`, `event.reason`, `event.wasClean`). (2) Hertzfeld added 13 new tests across 6 describe blocks: subprotocol auth verification, URL normalization (trailing slashes), close code diagnostics. Orchestration logs written for both agents. Decision merged to decisions.md with Status: Implemented. Risk assessment: Low (ticket-exchange still uses query param, only auth token moves to subprotocol). 524 tests passing (13 new), 15 skipped (TelemetryDrawer suite), build clean. Production-ready.
